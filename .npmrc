# pnpm: no re-ejecutar `pnpm install` automáticamente antes de cada `pnpm run`.
# En CI (Amplify/Docker) esa reinstalación puede fallar (EACCES / ignored-builds)
# y no es necesaria: las deps ya se instalan en el paso previo.
verify-deps-before-run=false
